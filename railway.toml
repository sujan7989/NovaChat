[build]
builder = "dockerfile"
dockerfilePath = "web/server/Dockerfile"
buildContext = "web/server"

[deploy]
startCommand = "node src/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
